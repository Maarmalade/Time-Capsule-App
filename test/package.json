{
  "name": "firestore-security-rules-tests",
  "version": "1.0.0",
  "description": "Security rules tests for Time Capsule Firestore",
  "scripts": {
    "test": "jest firestore-security-rules.test.js",
    "test:watch": "jest --watch firestore-security-rules.test.js"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.3",
    "firebase": "^10.7.1",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "setupFilesAfterEnv": ["<rootDir>/jest.setup.js"]
  }
}