// Export the UserProfile model for backward compatibility
export 'user_profile.dart';

// Import UserProfile for the typedef
import 'user_profile.dart';

// Legacy alias for UserProfile (if needed for existing code)
typedef UserModel = UserProfile;